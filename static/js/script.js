document.addEventListener('DOMContentLoaded', function() {
    // This function will run after the DOM is loaded. Add any interactivity or event listeners here.

    // Example: Display a message after successfully setting a budget (assuming you have a specific element for messages):
    // const messageBox = document.querySelector('.message-box');
    // if (messageBox && messageBox.textContent.includes('Budget updated successfully!')) {
    //     setTimeout(() => {
    //         messageBox.style.display = 'none';
    //     }, 3000);  // Hide the message after 3 seconds
    // }
});
